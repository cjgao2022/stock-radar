# 构建依赖：Python 3.12+ 的 venv 默认不再预装 setuptools，
# 而 akshare 的传递依赖 jsonpath 是 setup.py 老式包、构建时需要 setuptools。
# 放最前面，确保先于 akshare 安装。
setuptools
wheel
fastapi
uvicorn[standard]
akshare
requests
pyyaml
python-dotenv
apscheduler
jinja2
